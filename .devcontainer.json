{
    "extensions": [
		"ms-python.python",
		"bungcip.better-toml",
		"EditorConfig.EditorConfig",
		"ms-vscode.makefile-tools"
	],
    "postStartCommand": "pip install poetry --user && poetry update"
}
